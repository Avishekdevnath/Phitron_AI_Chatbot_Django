def string_length(string):
    """Counts the length of a string manually.

    Args:
        string: The string to count the length of.

    Returns:
        The length of the string.
    """
    length = 0
    for character in string:
        length += 1

    return length